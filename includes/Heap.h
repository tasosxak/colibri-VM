/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/* 
 * File:   Heap.h
 * Author: tasosxak
 *
 * Created on February 11, 2018, 6:52 PM
 */

#ifndef HEAP_H
#define HEAP_H

#include "Allocated_Object.h"


class Heap
{
    public:
        /*
        Heap(long memory);
        virtual ~Heap();

        int get_num_baby();
        int get_num_kid();
        int get_num_teen();
        int get_num_old();
        int get_num_permanent();

        Allocated_Object* get_baby(int index);
        Allocated_Object* get_kid(int index);
        Allocated_Object* get_teen(int index);
        Allocated_Object* get_old(int index);
        Allocated_Object* get_permanent(int index);

        int add_baby_object(Allocated_Object* obj);
        int add_kid_object(Allocated_Object* obj);
        int add_teen_object(Allocated_Object* obj);
        int add_old_object(Allocated_Object* obj);
        int add_permanent_object(Allocated_Object* obj);

        int delete_baby(int index);
        int delete_kid(int index);
        int delete_teen(int index);
        int delete_old(int index);

        int move_baby_to_kid(int index1, int index2);
        int move_kid_to_teen(int index1, int index2);
        int move_teen_to_kid(int index1, int index2);
        int move_teen_to_old(int index1, int index2);
        int move_old_to_permanent(int index1, int index2);

        long get_memory_space();
        long get_max_memory_space();
        void add_memory_space(long memory);

        int max_num_baby_objects;
        int max_num_kid_objects;
        int max_num_teen_objects;
        int max_num_old_objects;
        int max_num_permanent_objects;
*/
    protected:

    private:
/*
        long memory_space;
        long max_memory_space;

        int num_baby_objects;
        int num_kid_objects;
        int num_teen_objects;
        int num_old_objects;
        int num_permanent_objects;

        //bool* FREE_SPACE[];

       
        Allocated_Object* baby_objects[];
        Allocated_Object* kid_objects[];
        Allocated_Object* teen_objects[];

       
        Allocated_Object* old_objects[];

       
        Allocated_Object* permanent_objects[];
        */
};



#endif /* HEAP_H */

